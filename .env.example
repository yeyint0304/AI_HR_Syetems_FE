# Base URL of the HR System backend API (see scripts/.env.dev -> BE_ENDPOINT
# in the harness repo). Must be reachable from the browser since the
# Project pages call it via client-side fetch.
NEXT_PUBLIC_API_BASE_URL=http://131.153.49.2:5093
